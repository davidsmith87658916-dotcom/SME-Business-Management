"""Inventory routes are exposed by ``routers.products``.

Keeping this module as a documented compatibility placeholder prevents a second
inventory router from bypassing the centralized stock transaction rules.
"""
